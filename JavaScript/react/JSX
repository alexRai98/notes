## NOTES ##

-JSX elements always closed de tags
-Use className for a class
-Don't put quotes around curly braces 

i.e : const element = <img className="logo" src={user.avatarUrl}></img>;

## Use ##
import React from "react";
import ReactDOM from "react-dom"; // reder react in DOM

const name = 'Josh Perez';
const element = <h1>Hello, {name}</h1>;

ReactDOM.render(element,document.getElementById('root'));



